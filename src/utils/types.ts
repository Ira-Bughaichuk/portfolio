export type NavLink = {
    title:string;
    path:string;
}
export type SkillsList = {
    tech: string[];
    soft: string[];
};

//props

export type INavLinkProps = {
    title:string;
    path:string;
}
export type ITitleProps = {
    title:string,
  }